name: Build Android Apps

on:
  push:
    branches: [ main, develop ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Build type'
        required: true
        default: 'debug'
        type: choice
        options:
          - debug
          - release

jobs:
  build-default-theme:
    name: Build Default Theme App
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Set up Java JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: npm ci

      - name: Create required directories
        run: mkdir -p android/app/src/main/assets

      - name: Setup and Validate Keystore (Default)
        if: github.event.inputs.build_type == 'release' || startsWith(github.ref, 'refs/tags/')
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          set -e
          echo "=== Keystore Setup and Validation ==="
          
          # Check if all secrets are present
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "ERROR: KEYSTORE_BASE64 secret not found"
            exit 1
          fi
          if [ -z "$KEYSTORE_PASSWORD" ]; then
            echo "ERROR: KEYSTORE_PASSWORD secret not found"
            exit 1
          fi
          if [ -z "$KEY_ALIAS" ]; then
            echo "ERROR: KEY_ALIAS secret not found"
            exit 1
          fi
          if [ -z "$KEY_PASSWORD" ]; then
            echo "ERROR: KEY_PASSWORD secret not found"
            exit 1
          fi
          
          echo "✓ All keystore secrets are present"
          
          # Show Base64 string length (for debugging)
          echo "Base64 string length: ${#KEYSTORE_BASE64}"
          
          # Decode keystore
          echo "Decoding keystore..."
          echo "$KEYSTORE_BASE64" | base64 -d > android/app/release.keystore
          
          # Verify file was created and check size
          if [ ! -f android/app/release.keystore ]; then
            echo "ERROR: Keystore file was not created"
            exit 1
          fi
          
          FILE_SIZE=$(stat -c%s android/app/release.keystore)
          echo "✓ Keystore file created: $FILE_SIZE bytes"
          
          # Verify it's a valid keystore using keytool
          echo "Verifying keystore validity..."
          if keytool -list -keystore android/app/release.keystore -storepass "$KEYSTORE_PASSWORD" -alias "$KEY_ALIAS" > /dev/null 2>&1; then
            echo "✓ Keystore is valid and password is correct"
          else
            echo "ERROR: Failed to verify keystore. Either the file is corrupted or the password is incorrect"
            echo "Attempting to list keystore contents..."
            keytool -list -keystore android/app/release.keystore -storepass "$KEYSTORE_PASSWORD" || true
            exit 1
          fi
          
          # Show keystore info
          echo "=== Keystore Information ==="
          keytool -list -v -keystore android/app/release.keystore -storepass "$KEYSTORE_PASSWORD" -alias "$KEY_ALIAS" | head -20
          
          echo "=== Keystore setup completed successfully ==="

      - name: Sync Capacitor
        run: npm run cap:sync

      - name: Grant execute permission for gradlew
        run: chmod +x android/gradlew

      - name: Build Debug APK
        if: github.event.inputs.build_type != 'release' && !startsWith(github.ref, 'refs/tags/')
        run: |
          cd android
          ./gradlew assembleDebug --stacktrace

      - name: Build Release APK
        if: github.event.inputs.build_type == 'release' || startsWith(github.ref, 'refs/tags/')
        env:
          KEYSTORE_FILE: ${{ github.workspace }}/android/app/release.keystore
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          cd android
          ./gradlew assembleRelease --stacktrace

      - name: Upload Debug APK
        if: github.event.inputs.build_type != 'release' && !startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: expense-tracker-default-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30

      - name: Upload Release APK
        if: github.event.inputs.build_type == 'release' || startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: expense-tracker-default-release
          path: android/app/build/outputs/apk/release/app-release-unsigned.apk
          retention-days: 90

  build-vibe-theme:
    name: Build Vibe Finance App
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Set up Java JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: npm ci

      - name: Create required directories
        run: mkdir -p android/app/src/main/assets

      - name: Update Android config for Vibe theme
        run: |
          # Update build.gradle
          sed -i 's/namespace "com.expensetracker.app"/namespace "com.vibefinance.app"/g' android/app/build.gradle
          sed -i 's/applicationId "com.expensetracker.app"/applicationId "com.vibefinance.app"/g' android/app/build.gradle
          
          # Update strings.xml
          sed -i 's/<string name="app_name">Expense Tracker<\/string>/<string name="app_name">Vibe Finance<\/string>/g' android/app/src/main/res/values/strings.xml
          sed -i 's/<string name="title_activity_main">Expense Tracker<\/string>/<string name="title_activity_main">Vibe Finance<\/string>/g' android/app/src/main/res/values/strings.xml
          sed -i 's/<string name="package_name">com.expensetracker.app<\/string>/<string name="package_name">com.vibefinance.app<\/string>/g' android/app/src/main/res/values/strings.xml
          sed -i 's/<string name="custom_url_scheme">com.expensetracker.app<\/string>/<string name="custom_url_scheme">com.vibefinance.app<\/string>/g' android/app/src/main/res/values/strings.xml

      - name: Sync Capacitor with Vibe config
        run: |
          # Temporarily use Vibe config as default
          cp capacitor.vibe.config.ts capacitor.config.ts
          npx cap sync
          git checkout capacitor.config.ts

      - name: Grant execute permission for gradlew
        run: chmod +x android/gradlew

      - name: Build Debug APK
        if: github.event.inputs.build_type != 'release' && !startsWith(github.ref, 'refs/tags/')
        run: |
          cd android
          ./gradlew assembleDebug --stacktrace

      - name: Decode and setup keystore (Vibe)
        if: github.event.inputs.build_type == 'release' || startsWith(github.ref, 'refs/tags/')
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64_VIBE }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            printf '%s' "$KEYSTORE_BASE64" | base64 -d > android/app/release.keystore
            ls -lh android/app/release.keystore
            file android/app/release.keystore
            echo "Keystore decoded successfully"
          else
            echo "Warning: KEYSTORE_BASE64_VIBE secret not found. Using default keystore."
            if [ -n "${{ secrets.KEYSTORE_BASE64 }}" ]; then
              printf '%s' "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android/app/release.keystore
              Setup and Validate Keystore (Vibe)
        if: github.event.inputs.build_type == 'release' || startsWith(github.ref, 'refs/tags/')
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64_VIBE || secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD_VIBE || secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS_VIBE || secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD_VIBE || secrets.KEY_PASSWORD }}
        run: |
          set -e
          echo "=== Keystore Setup and Validation (Vibe) ==="
          
          # Check if all secrets are present
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "ERROR: KEYSTORE_BASE64_VIBE secret not found"
            exit 1
          fi
          if [ -z "$KEYSTORE_PASSWORD" ]; then
            echo "ERROR: KEYSTORE_PASSWORD_VIBE secret not found"
            exit 1
          fi
          if [ -z "$KEY_ALIAS" ]; then
            echo "ERROR: KEY_ALIAS_VIBE secret not found"
            exit 1
          fi
          if [ -z "$KEY_PASSWORD" ]; then
            echo "ERROR: KEY_PASSWORD_VIBE secret not found"
            exit 1
          fi
          
          echo "✓ All keystore secrets are present"
          
          # Show Base64 string length (for debugging)
          echo "Base64 string length: ${#KEYSTORE_BASE64}"
          
          # Decode keystore
          echo "Decoding keystore..."
          echo "$KEYSTORE_BASE64" | base64 -d > android/app/release.keystore
          
          # Verify file was created and check size
          if [ ! -f android/app/release.keystore ]; then
            echo "ERROR: Keystore file was not created"
            exit 1
          fi
          
          FILE_SIZE=$(stat -c%s android/app/release.keystore)
          echo "✓ Keystore file created: $FILE_SIZE bytes"
          
          # Verify it's a valid keystore using keytool
          echo "Verifying keystore validity..."
          if keytool -list -keystore android/app/release.keystore -storepass "$KEYSTORE_PASSWORD" -alias "$KEY_ALIAS" > /dev/null 2>&1; then
            echo "✓ Keystore is valid and password is correct"
          else
            echo "ERROR: Failed to verify keystore. Either the file is corrupted or the password is incorrect"
            echo "Attempting to list keystore contents..."
            keytool -list -keystore android/app/release.keystore -storepass "$KEYSTORE_PASSWORD" || true
            exit 1
          fi
          
          # Show keystore info
          echo "=== Keystore Information ==="
          keytool -list -v -keystore android/app/release.keystore -storepass "$KEYSTORE_PASSWORD" -alias "$KEY_ALIAS" | head -20
          
          echo "=== Keystore setup completed successfully ==="github.event.inputs.build_type != 'release' && !startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: vibe-finance-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30

      - name: Upload Release APK
        if: github.event.inputs.build_type == 'release' || startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: vibe-finance-release
          path: android/app/build/outputs/apk/release/app-release-unsigned.apk
          retention-days: 90

  create-release:
    name: Create GitHub Release
    needs: [build-default-theme, build-vibe-theme]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/expense-tracker-default-release/app-release-unsigned.apk
            artifacts/vibe-finance-release/app-release-unsigned.apk
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
